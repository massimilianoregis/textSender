# textSender

Android SMS JSON gateway. The application reads SMS jobs from a configured HTTP endpoint, sends them through the phone SIM, and reports OK/KO to a configurable callback endpoint.

The full Android project is developed on a feature branch and built by GitHub Actions.
